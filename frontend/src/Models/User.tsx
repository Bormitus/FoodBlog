export type UserProfileToken = {
    username: string;
    email: string;
    token: string;
    role: string;
}

export type UserProfile = {
    userName: string;
    email: string;
    role: string;
}